const StartBot = require('./task/startBot')
const Message = require('./task/message')
const fixed = require('./config/fixed')


StartBot('zeki')
/*
const msg = {
  text: () => '广东天河天气',
  say: (text) => console.log(text)
};

Message(msg)*/
